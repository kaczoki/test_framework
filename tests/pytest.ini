[pytest]
env =
    GITHUB_API_KEY=KEY
    GITHUB_USERNAME=USERNAME
    GITHUB_PASSWORD=PASSWORD
    BASE_URL=https://api.github.com
    BASE_URL_GUI=https://github.com
    RECORD_VIDEO=true
addopts = --alluredir=./results
markers =
    smoke: smoke tests
    regression: regression tests
